#!/usr/bin/python3
def element_at(my_list,idx):
    """retrive an element

    Args:
        mylist: list to print
        idx: the position

    Returns:
        The return nothing
    """
    if(idx < 0):
        return none
    elif(idx > len(my_list)):
        return none
    else :
        return my_list[idx]
